@model IEnumerable<Usuario>

@{
    ViewBag.Title = "Todos los Usuarios";
}

<div class="container-fluid fondo" >

    <h1>  Usuarios</h1>

    @if (Model.Any())
    {
        <a asp-action="Register" asp-controller="Account"
           class="font2" style="width:auto">
            Agregar Usuario
        </a>
    <table class="table-striped">
        <thead class="table-dark font2">
            <tr>
                <td>Usuarios</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td class="font2">
                        @user.UserName
                    </td>
                    <td>
                        <a asp-action="EditUser" asp-controller="Administration"
                           asp-route-id="@user.Id" class="font2">Editar</a>|
                        <a asp-action="DeleteUser" asp-controller="Administration"
                           asp-route-id="@user.Id" class="font2">Eliminar</a>
                    </td>
                </tr>
                @*<div class="card mb-3">
                        <div class="card-header" style="display:none">
                            User Id : @user.Id
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@user.UserName</h5>
                        </div>
                        <div class="card-footer">
                            <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post">


                                <span id="confirmDeleteSpan_@user.Id" style="display:none">
                                    <span> Esta seguro de eliminar usuario?</span>
                                    <button type="submit" class="btn btn-danger">Si</button>
                                    <a href="#" class="btn btn-primary font2"
                                       onclick="confirmDelete('@user.Id', false)">No</a>
                                </span>

                                <span id="deleteSpan_@user.Id">
                                    <a href="#" class="btn btn-danger"
                                       onclick="confirmDelete('@user.Id', true)">Eliminar</a>
                                </span>
                            </form>
                        </div>
                    </div>*@
            }
        </tbody>
    </table>
    }
</div>



@section Scripts {
    <script src="~/js/CustomScript.js"></script>
}