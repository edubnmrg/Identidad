@model Identidad.Models.Recursos.Provincia

@{
    ViewData["Title"] = "Detalle Provincia";
}

<h1>Detalle Provincia</h1>

<div class="font2">
    <h4>Provincia</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2 font2b">
            @Html.DisplayNameFor(model => model.nombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.nombre)
        </dd>
    </dl>
</div>
<div>
    <a class="font2 " asp-action="Edit" asp-route-id="@Model.ID">Editar</a> |
    <a class="font2 " asp-action="Index">Volver a la Lista</a>
</div>
